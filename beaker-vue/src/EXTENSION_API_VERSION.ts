// Public contract version exposed to extensions. Bumped only when the
// contract surface changes (externalized package list, public API
// removal/rename, host-installed plugin set, SemVer-major Vue/PrimeVue).
// See plans/beaker-ui-split.md for the versioning policy.
export const EXTENSION_API_VERSION = "1.0";
